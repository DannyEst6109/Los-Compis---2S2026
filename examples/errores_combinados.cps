let datos: integer[] = [10, 20, 30];
let posicion: integer = 0

foreach (item in datos {
  print(item @ posicion);
  posicion = posicion + 1;
}

const mensaje: string = ;
$resultado = datos[0];
print("fin");
